// import { applyDecorators, SetMetadata, UseGuards } from '@nestjs/common';

// import { AdministratorRoles } from 'src/administrator/schemas';
// // import { JwtGuard } from 'src/authentication/guards';

// import { ALLOWED_ROLES } from './constants';

// export const AllowedRoles = (roles: AdministratorRoles[]) => {
//   return applyDecorators(
//     SetMetadata(ALLOWED_ROLES, roles),
//     UseGuards(JwtGuard),
//   );
// };
